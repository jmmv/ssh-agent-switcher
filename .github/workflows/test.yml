name: Test

on: push

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - run: ./.github/workflows/install-shtk.sh 1.7 /usr/local
            - run: ./.github/workflows/distcheck.sh
